//Main.java

//Display number of occurences of an element in array

import java.util.Scanner;
public class Main
{
    public static void main(String[] args) {
       int[] numbers = {1, 1, 2, 3, 3, 3, 6};
       System.out.println("Enter the search Element: ");
       Scanner s = new Scanner(System.in);
       int searchElement = s.nextInt();
       int occ = 0;
       
       for(int i=0; i<numbers.length; i++){
           if(searchElement == numbers[i])
                occ++;
       }
       System.out.println("The number of occurences of given element is: "+occ);
       
    }
}

o/p:

Enter the search Element: 

3
The number of occurences of given element is: 3
